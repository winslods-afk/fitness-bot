# 📝 Git команды для загрузки проекта на GitHub

## ⚠️ ВАЖНО: Перезапустите PowerShell после установки Git!

После установки Git **обязательно закройте и откройте PowerShell заново**, чтобы команды git стали доступны.

---

## Шаг 1: Перезапустите PowerShell

1. Закройте текущее окно PowerShell
2. Откройте новое окно PowerShell
3. Перейдите в папку проекта:
   ```powershell
   cd C:\Users\Roman\fitness-bot
   ```

## Шаг 2: Проверьте, что Git работает

```powershell
git --version
```

Должна появиться версия Git (например: `git version 2.51.2`)

## Шаг 3: Настройте Git (первый раз)

```powershell
git config --global user.name "winslods-afk"
git config --global user.email "your.email@example.com"
```

(Замените email на ваш реальный)

## Шаг 4: Инициализируйте репозиторий

```powershell
git init
```

## Шаг 5: Добавьте все файлы

```powershell
git add .
```

## Шаг 6: Создайте первый коммит

```powershell
git commit -m "Initial commit: Fitness bot"
```

## Шаг 7: Добавьте удаленный репозиторий

```powershell
git remote add origin https://github.com/winslods-afk/fitness-bot.git
```

## Шаг 8: Переименуйте ветку в main (если нужно)

```powershell
git branch -M main
```

## Шаг 9: Загрузите код на GitHub

```powershell
git push -u origin main
```

**Примечание:** При первом push GitHub может попросить авторизацию. Следуйте инструкциям на экране.

---

## 🔐 Авторизация в GitHub

Если Git запросит авторизацию:

1. **Через браузер (рекомендуется):**
   - Git откроет браузер
   - Войдите в GitHub
   - Разрешите доступ

2. **Через Personal Access Token:**
   - Создайте токен на GitHub: Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Используйте токен вместо пароля

---

## ✅ Проверка

После успешного push зайдите на:
https://github.com/winslods-afk/fitness-bot

Вы должны увидеть все файлы проекта!

---

## 🚀 Дальше: Деплой на Railway

После загрузки кода на GitHub:

1. Зайдите на [railway.app](https://railway.app)
2. "Start a New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий `fitness-bot`
4. Добавьте переменную `TELEGRAM_BOT_TOKEN` в Railway
5. Готово!

---

## ❓ Проблемы?

**"git: command not found"**
- Перезапустите PowerShell
- Проверьте установку: `winget list Git.Git`

**"Permission denied"**
- Проверьте, что репозиторий создан на GitHub
- Убедитесь, что вы авторизованы в GitHub

**"Repository not found"**
- Убедитесь, что репозиторий `fitness-bot` создан на GitHub
- Проверьте правильность URL: `https://github.com/winslods-afk/fitness-bot.git`

