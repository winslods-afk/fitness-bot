# 🔧 Исправление: Политика выполнения скриптов PowerShell

Если вы получили ошибку:
```
railway : Невозможно загрузить файл ... так как выполнение сценариев отключено в этой системе
```

Это означает, что в Windows по умолчанию отключено выполнение скриптов PowerShell из соображений безопасности.

## ✅ Решение 1: Изменить политику выполнения (рекомендуется)

### Шаг 1: Откройте PowerShell от имени администратора

1. Нажмите `Win + X`
2. Выберите **Windows PowerShell (администратор)** или **Терминал (администратор)**

### Шаг 2: Измените политику выполнения

Выполните одну из команд:

**Для текущего пользователя (рекомендуется):**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**Для всей системы (требует прав администратора):**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope LocalMachine
```

### Шаг 3: Подтвердите изменение

Нажмите `Y` (Yes) когда система спросит подтверждение.

### Шаг 4: Проверьте

```powershell
railway --version
```

Должно работать без ошибок.

---

## ✅ Решение 2: Использовать Command Prompt (cmd)

Вместо PowerShell используйте обычную командную строку:

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. Выполните команды:

```cmd
railway login
cd C:\Users\Roman\fitness-bot
railway link
python sync_db.py
```

---

## ✅ Решение 3: Использовать npx (без глобальной установки)

Если не хотите менять политику выполнения, можно использовать `npx`:

```powershell
npx @railway/cli login
npx @railway/cli link
npx @railway/cli run cat fitness_bot.db > fitness_bot_remote.db
```

**Недостаток:** Нужно добавлять `npx` перед каждой командой.

---

## 📋 Объяснение политик выполнения

- **Restricted** (по умолчанию) — скрипты не выполняются
- **RemoteSigned** (рекомендуется) — локальные скрипты выполняются, удаленные требуют подписи
- **Unrestricted** (не рекомендуется) — все скрипты выполняются без проверки

**RemoteSigned** — безопасный вариант, который позволяет выполнять локальные скрипты (как Railway CLI).

---

## ⚠️ Если команда не работает

Если `Set-ExecutionPolicy` не работает, попробуйте:

1. **Запустить PowerShell от имени администратора**
2. **Использовать полную команду:**
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser -Force
   ```

3. **Проверить текущую политику:**
   ```powershell
   Get-ExecutionPolicy -List
   ```

---

## 🔍 Проверка после исправления

После изменения политики выполните:

```powershell
# Проверка политики
Get-ExecutionPolicy

# Проверка Railway CLI
railway --version

# Вход в Railway
railway login

# Переход в папку проекта
cd C:\Users\Roman\fitness-bot

# Подключение проекта
railway link

# Синхронизация БД
python sync_db.py
```

---

**Рекомендация:** Используйте **Решение 1** (изменить политику выполнения) — это самый удобный способ для постоянной работы с Railway CLI.

