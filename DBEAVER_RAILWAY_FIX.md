# Исправление ошибки "Invalid JDBC URL" в DBeaver для Railway

## Проблема

При попытке подключиться к PostgreSQL на Railway через DBeaver вы получаете ошибку:
```
Invalid JDBC URL: postgresql://postgres:password@switchback.proxy.rlwy.net:14062/railway
```

## Причина

DBeaver не может использовать JDBC URL напрямую. Нужно заполнить поля подключения **вручную**, а не вставлять URL в одно поле.

## Решение

### Шаг 1: Получите DATABASE_PUBLIC_URL

```bash
# Через Railway CLI
railway service  # Выберите PostgreSQL
railway variables get DATABASE_PUBLIC_URL

# Или через Railway Dashboard
# PostgreSQL сервис → Variables → DATABASE_PUBLIC_URL
```

Пример:
```
postgresql://postgres:dVbogRUfVgauuoQVglvnUizZhcYDiGBY@switchback.proxy.rlwy.net:14062/railway
```

### Шаг 2: Парсинг DATABASE_PUBLIC_URL

Разберите URL на компоненты:

```
postgresql://postgres:dVbogRUfVgauuoQVglvnUizZhcYDiGBY@switchback.proxy.rlwy.net:14062/railway
```

- **Пользователь:** `postgres` (после `//`, до `:`)
- **Пароль:** `dVbogRUfVgauuoQVglvnUizZhcYDiGBY` (после `:`, до `@`)
- **Хост:** `switchback.proxy.rlwy.net` (после `@`, до `:`)
- **Порт:** `14062` (после `:`, до `/`)
- **База данных:** `railway` (после `/`)

### Шаг 3: Создание подключения в DBeaver

1. **Откройте DBeaver**

2. **Создайте новое подключение:**
   - Нажмите `Ctrl+Shift+N` или иконку "Новое подключение"
   - Выберите **"PostgreSQL"**
   - Нажмите **"Далее"**

3. **Заполните поля ВРУЧНУЮ (не используйте JDBC URL!):**

   **Вкладка "Главное":**
   - ✅ **Хост:** `switchback.proxy.rlwy.net`
   - ✅ **Порт:** `14062`
   - ✅ **База данных:** `railway`
   - ✅ **Пользователь:** `postgres`
   - ✅ **Пароль:** `dVbogRUfVgauuoQVglvnUizZhcYDiGBY`
   - ✅ **Сохранить пароль** (поставьте галочку)

   **Вкладка "SSL" (ОБЯЗАТЕЛЬНО!):**
   - ✅ **Использовать SSL** (поставьте галочку)
   - **Режим SSL:** выберите **"require"** или **"allow"**

4. **Проверка подключения:**
   - Нажмите **"Тест подключения"**
   - Если появится запрос на скачивание драйвера, нажмите **"Скачать"**
   - Если все правильно, появится **"Соединение установлено"**

5. **Сохранение:**
   - Нажмите **"Готово"**
   - Подключение появится в левой панели

## Важные моменты

### ❌ НЕ делайте так:
- НЕ вставляйте JDBC URL в одно поле
- НЕ используйте внутренний хост `*.railway.internal`
- НЕ забывайте включить SSL

### ✅ Делайте так:
- Заполняйте поля вручную
- Используйте `DATABASE_PUBLIC_URL` с хостом `*.proxy.rlwy.net`
- Обязательно включайте SSL

## Альтернативное решение: Использование Railway CLI

Если DBeaver все еще не работает, используйте Railway CLI:

```bash
# Подключение через Railway CLI
railway service  # Выберите PostgreSQL
railway connect postgres
```

Это откроет интерактивную сессию PostgreSQL в терминале, где вы можете выполнять SQL запросы.

## Проверка подключения

После успешного подключения:

1. **Разверните подключение в левой панели:**
   - Базы данных → `railway` → Схемы → `public` → Таблицы

2. **Проверьте таблицы:**
   - Должны быть таблицы: `users`, `sessions`, `exercises`, и т.д.

3. **Выполните тестовый запрос:**
   ```sql
   SELECT * FROM users;
   ```

## Решение проблем

### Ошибка: "SSL connection required"

**Решение:**
1. Перейдите на вкладку **"SSL"**
2. Поставьте галочку **"Использовать SSL"**
3. Выберите режим SSL: **"require"**
4. Нажмите **"Тест подключения"** снова

### Ошибка: "password authentication failed"

**Решение:**
1. Проверьте, что вы правильно скопировали пароль из `DATABASE_PUBLIC_URL`
2. Убедитесь, что нет лишних пробелов
3. Попробуйте получить `DATABASE_PUBLIC_URL` заново

### Ошибка: "Connection timeout"

**Решение:**
1. Проверьте, что PostgreSQL сервис запущен в Railway
2. Убедитесь, что вы используете `DATABASE_PUBLIC_URL`, а не внутренний URL
3. Попробуйте перезапустить PostgreSQL сервис через Railway Dashboard

### Ошибка: "Driver not found"

**Решение:**
1. При первом подключении DBeaver предложит скачать драйвер
2. Нажмите **"Скачать"** и дождитесь установки
3. Попробуйте подключиться снова

## Готово! 🎉

Теперь вы можете:
- ✅ Просматривать данные в базе данных
- ✅ Выполнять SQL запросы
- ✅ Редактировать данные
- ✅ Экспортировать данные

**Удачной работы с базой данных!**

