# Подключение к базе данных Railway через DBeaver

## 📋 Что нужно

1. **DBeaver** (бесплатный инструмент для работы с базами данных)
2. **DATABASE_URL** из Railway (получается автоматически)
3. **Доступ к проекту Railway**

## 🚀 Пошаговая инструкция

### Шаг 1: Установка DBeaver

1. **Скачайте DBeaver:**
   - Перейдите на https://dbeaver.io/
   - Нажмите **"Download"**
   - Выберите **"DBeaver Community Edition"** (бесплатная версия)
   - Скачайте установщик для Windows

2. **Установите DBeaver:**
   - Запустите установщик
   - Следуйте инструкциям установки
   - Запустите DBeaver

### Шаг 2: Получение DATABASE_URL из Railway

⚠️ **ВАЖНО:** Нужен **публичный** DATABASE_URL, а не внутренний!

#### Вариант A: Через Railway Dashboard (рекомендуется)

1. **Откройте Railway Dashboard:**
   - Перейдите на https://railway.app
   - Выберите ваш проект

2. **Откройте PostgreSQL сервис:**
   - Нажмите на сервис **PostgreSQL** (не на сервис бота!)
   - Откройте вкладку **"Variables"** или **"Connect"**

3. **Найдите публичный DATABASE_URL:**
   - Ищите переменную `DATABASE_URL`
   - Или перейдите на вкладку **"Connect"** → **"Postgres Connection URL"**
   - **Убедитесь, что хост содержит `*.railway.app` или `*.up.railway.app`, а НЕ `*.railway.internal`**

4. **Публичный хост выглядит так:**
   ```
   postgresql://postgres:password@containers-us-west-xxx.railway.app:5432/railway
   ```
   ✅ Это правильный формат для DBeaver

5. **Внутренний хост (НЕ подходит для DBeaver):**
   ```
   postgresql://postgres:password@postgres.railway.internal:5432/railway
   ```
   ❌ Этот хост доступен только внутри Railway

#### Вариант B: Через Railway CLI

```bash
# Перейдите в папку проекта
cd C:\Users\Roman\fitness-bot

# Выберите PostgreSQL сервис (важно!)
railway service  # Выберите PostgreSQL из списка

# Получите DATABASE_URL
railway variables get DATABASE_URL
```

**Важно:** Убедитесь, что вы выбрали **PostgreSQL сервис**, а не сервис бота!

#### Если получаете внутренний хост (`*.railway.internal`)

Если вы получаете хост с `*.railway.internal`, это внутренний хост. Для DBeaver нужен публичный хост:
- Используйте Railway Dashboard → PostgreSQL сервис → вкладка "Connect"
- Или см. подробную инструкцию: [RAILWAY_PUBLIC_DATABASE_URL.md](RAILWAY_PUBLIC_DATABASE_URL.md)

### Шаг 3: Парсинг DATABASE_URL

Формат `DATABASE_URL`:
```
postgresql://username:password@host:port/database
```

Пример:
```
postgresql://postgres:abc123xyz@containers-us-west-123.railway.app:5432/railway
```

**Расшифровка:**
- **Username (пользователь):** `postgres` (часть после `//` и до `:`)
- **Password (пароль):** `abc123xyz` (часть после `:` и до `@`)
- **Host (хост):** `containers-us-west-123.railway.app` (часть после `@` и до `:`)
- **Port (порт):** `5432` (часть после `:` и до `/`)
- **Database (база данных):** `railway` (часть после `/`)

### Шаг 4: Создание подключения в DBeaver

1. **Откройте DBeaver**

2. **Создайте новое подключение:**
   - Нажмите на иконку **"Новое подключение"** (плагин с плюсом) в левом верхнем углу
   - Или меню **"База данных"** → **"Новое подключение"**
   - Или нажмите `Ctrl+Shift+N`

3. **Выберите тип базы данных:**
   - В списке найдите **"PostgreSQL"**
   - Выберите его
   - Нажмите **"Далее"**

4. **Заполните данные подключения:**

   **Вкладка "Главное":**
   - **Хост (Host):** `containers-us-west-123.railway.app` (из DATABASE_URL)
   - **Порт (Port):** `5432` (из DATABASE_URL, обычно 5432)
   - **База данных (Database):** `railway` (из DATABASE_URL)
   - **Пользователь (Username):** `postgres` (из DATABASE_URL)
   - **Пароль (Password):** `abc123xyz` (из DATABASE_URL)
   - ✅ **Сохранить пароль** (поставьте галочку, чтобы не вводить каждый раз)

   **Вкладка "SSL":**
   - ✅ **Использовать SSL** (поставьте галочку)
   - **Режим SSL:** выберите **"require"** или **"prefer"**

5. **Проверка подключения:**
   - Нажмите **"Тест подключения"** (кнопка внизу)
   - Если все правильно, появится сообщение **"Соединение установлено"**
   - Если появилась ошибка, см. раздел "Решение проблем" ниже

6. **Сохранение подключения:**
   - Нажмите **"Готово"** или **"Finish"**
   - Подключение появится в левой панели

### Шаг 5: Использование DBeaver

1. **Откройте подключение:**
   - В левой панели найдите ваше подключение
   - Разверните его (нажмите на стрелку)
   - Разверните **"Базы данных"** → ваша база данных
   - Разверните **"Схемы"** → **"public"** → **"Таблицы"**

2. **Просмотр таблиц:**
   - Вы увидите список таблиц: `users`, `sessions`, `exercises`, и т.д.
   - Двойной клик на таблицу откроет данные

3. **Выполнение SQL запросов:**
   - Нажмите на иконку **"SQL Editor"** (иконка с SQL) в верхней панели
   - Или нажмите `Ctrl+` (обратная кавычка)
   - Введите SQL запрос, например:
     ```sql
     SELECT * FROM users;
     ```
   - Нажмите `Ctrl+Enter` для выполнения запроса

4. **Просмотр данных:**
   - Результаты запроса появятся в нижней панели
   - Вы можете редактировать данные прямо в таблице
   - Изменения сохраняются автоматически (или нажмите `Ctrl+S`)

## 🔍 Полезные SQL запросы

### Просмотр всех пользователей:
```sql
SELECT * FROM users;
```

### Просмотр всех программ:
```sql
SELECT s.name, s.created_at, u.telegram_id
FROM sessions s
JOIN users u ON s.user_id = u.id;
```

### Просмотр упражнений:
```sql
SELECT e.name, e.exercise_id, wd.name as day_name
FROM exercises e
JOIN workout_days wd ON e.workout_day_id = wd.id;
```

### Просмотр выполненных подходов:
```sql
SELECT 
    e.name as exercise_name,
    ps.weight,
    ps.reps,
    ps.timestamp,
    u.telegram_id
FROM performed_sets ps
JOIN exercises e ON ps.exercise_id = e.exercise_id
JOIN session_runs sr ON ps.session_run_id = sr.id
JOIN users u ON sr.user_id = u.id
ORDER BY ps.timestamp DESC
LIMIT 100;
```

### Статистика по пользователю:
```sql
SELECT 
    u.telegram_id,
    COUNT(DISTINCT s.session_id) as programs_count,
    COUNT(DISTINCT ps.id) as workouts_count
FROM users u
LEFT JOIN sessions s ON u.id = s.user_id
LEFT JOIN session_runs sr ON s.session_id = sr.session_id
LEFT JOIN performed_sets ps ON sr.id = ps.session_run_id
GROUP BY u.telegram_id;
```

## 🛠️ Решение проблем

### Ошибка: "Invalid JDBC URL" или "Connection refused"

**Причина 1:** Используется внутренний хост `*.railway.internal`

**Решение:**
1. Убедитесь, что вы используете **публичный** DATABASE_URL (хост должен содержать `*.railway.app`, а не `*.railway.internal`)
2. Получите публичный DATABASE_URL через Railway Dashboard → PostgreSQL сервис → вкладка "Connect"
3. Или см. подробную инструкцию: [RAILWAY_PUBLIC_DATABASE_URL.md](RAILWAY_PUBLIC_DATABASE_URL.md)

**Причина 2:** Неправильный хост или порт

**Решение:**
1. Проверьте, что вы правильно скопировали `DATABASE_URL`
2. Убедитесь, что хост и порт указаны правильно
3. Убедитесь, что вы выбрали **PostgreSQL сервис** при получении DATABASE_URL через CLI

### Ошибка: "password authentication failed"

**Причина:** Неправильный пароль или username

**Решение:**
1. Проверьте, что вы правильно скопировали `DATABASE_URL`
2. Убедитесь, что пароль и username указаны правильно
3. Попробуйте получить `DATABASE_URL` заново через `railway variables get DATABASE_URL`

### Ошибка: "SSL connection required"

**Причина:** Не включен SSL

**Решение:**
1. В настройках подключения перейдите на вкладку **"SSL"**
2. Поставьте галочку **"Использовать SSL"**
3. Выберите режим SSL: **"require"** или **"prefer"**
4. Нажмите **"Тест подключения"** снова

### Ошибка: "database does not exist"

**Причина:** Неправильное имя базы данных

**Решение:**
1. Проверьте, что вы правильно скопировали имя базы данных из `DATABASE_URL`
2. Убедитесь, что PostgreSQL сервис создан в Railway
3. Попробуйте получить `DATABASE_URL` заново через `railway variables get DATABASE_URL`

### Ошибка: "driver not found" или "PostgreSQL driver not installed"

**Причина:** Драйвер PostgreSQL не установлен

**Решение:**
1. В DBeaver при создании подключения выберите **"PostgreSQL"**
2. DBeaver автоматически предложит скачать драйвер
3. Нажмите **"Скачать"** и дождитесь установки
4. Попробуйте подключиться снова

### Не вижу таблицы

**Причина:** Таблицы еще не созданы

**Решение:**
1. Убедитесь, что бот запущен и база данных инициализирована
2. Проверьте логи Railway: `railway logs`
3. Должно быть сообщение: `База данных инициализирована`
4. Если таблиц нет, перезапустите бота через Railway Dashboard

## 💡 Полезные советы

1. **Сохраните пароль:** При создании подключения поставьте галочку "Сохранить пароль", чтобы не вводить его каждый раз

2. **Используйте SSL:** Railway требует SSL подключение, поэтому обязательно включите его

3. **Редактирование данных:** Вы можете редактировать данные прямо в таблице в DBeaver, изменения сохраняются автоматически

4. **Экспорт данных:** Вы можете экспортировать данные в различные форматы (CSV, Excel, JSON) через меню правой кнопки мыши на таблице

5. **Резервное копирование:** Вы можете создать бэкап базы данных через DBeaver: правый клик на базу данных → **"Инструменты"** → **"Создать резервную копию"**

6. **Автообновление:** DBeaver может автоматически обновлять данные в таблицах. Настройте это в настройках подключения

## 📚 Дополнительные ресурсы

- **Документация DBeaver:** https://dbeaver.com/docs/
- **Документация Railway:** https://docs.railway.app/
- **Подробная инструкция по настройке PostgreSQL:** [RAILWAY_POSTGRESQL_SETUP.md](RAILWAY_POSTGRESQL_SETUP.md)

## ✅ Готово!

Теперь вы можете:
- ✅ Просматривать данные в базе данных
- ✅ Выполнять SQL запросы
- ✅ Редактировать данные
- ✅ Экспортировать данные
- ✅ Создавать резервные копии

**Удачной работы с базой данных! 🎉**

