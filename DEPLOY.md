# 🚀 Деплой бота на хостинг - Пошаговая инструкция

Этот документ содержит инструкции по развертыванию бота на различных хостингах.

## 📋 Содержание

1. [Railway (рекомендуется)](#railway-рекомендуется)
2. [Render](#render)
3. [PythonAnywhere](#pythonanywhere)
4. [Fly.io](#flyio)
5. [VPS (DigitalOcean, AWS)](#vps-digitalocean-aws)

---

## 🚂 Railway (рекомендуется)

**Плюсы:** Бесплатный тариф, простой деплой, автоматические обновления

### Шаг 1: Подготовка проекта

1. Убедитесь, что у вас есть аккаунт на [Railway](https://railway.app)
2. Установите Railway CLI (опционально):
   ```bash
   npm i -g @railway/cli
   ```

### Шаг 2: Создание проекта на Railway

**Вариант A: Через веб-интерфейс**

1. Зайдите на [railway.app](https://railway.app) и войдите через GitHub
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Подключите ваш репозиторий (или создайте новый)
5. Railway автоматически определит Python проект

**Вариант B: Через CLI**

```bash
railway login
railway init
railway up
```

### Шаг 3: Настройка переменных окружения

1. В проекте Railway перейдите в "Variables"
2. Добавьте переменную:
   - `TELEGRAM_BOT_TOKEN` = ваш токен от BotFather

### Шаг 4: Настройка запуска

Railway автоматически определит команду запуска из структуры проекта.

Если нужно указать явно, создайте файл `Procfile`:
```
web: python -m app.main
```

### Шаг 5: Деплой

Railway автоматически задеплоит проект при пуше в GitHub.

Или вручную:
```bash
git push railway main
```

---

## 🎨 Render

**Плюсы:** Бесплатный тариф, простой интерфейс

### Шаг 1: Подготовка

1. Зайдите на [render.com](https://render.com) и зарегистрируйтесь
2. Подключите ваш GitHub аккаунт

### Шаг 2: Создание Web Service

1. Нажмите "New +" → "Web Service"
2. Подключите ваш репозиторий
3. Настройки:
   - **Name:** fitness-bot
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python -m app.main`

### Шаг 3: Переменные окружения

В разделе "Environment Variables" добавьте:
- `TELEGRAM_BOT_TOKEN` = ваш токен

### Шаг 4: Деплой

Нажмите "Create Web Service" - Render автоматически задеплоит проект.

---

## 🐍 PythonAnywhere

**Плюсы:** Бесплатный тариф для начинающих, простой интерфейс

### Шаг 1: Регистрация

1. Зарегистрируйтесь на [pythonanywhere.com](https://www.pythonanywhere.com)

### Шаг 2: Загрузка файлов

1. Откройте "Files"
2. Загрузите все файлы проекта через веб-интерфейс или через Git:
   ```bash
   git clone https://github.com/yourusername/fitness-bot.git
   ```

### Шаг 3: Настройка виртуального окружения

1. Откройте "Consoles" → "Bash"
2. Создайте виртуальное окружение:
   ```bash
   python3.10 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

### Шаг 4: Настройка переменных окружения

1. Создайте файл `.env` в корне проекта
2. Добавьте токен:
   ```
   TELEGRAM_BOT_TOKEN=your_token_here
   ```

### Шаг 5: Настройка задачи (Task)

1. Откройте "Tasks"
2. Создайте новую задачу:
   - **Command:** `cd /home/yourusername/fitness-bot && source venv/bin/activate && python -m app.main`
   - **Hour:** * (каждый час для проверки)
   - Или используйте "Always-on task" (платно)

**Примечание:** На бесплатном тарифе PythonAnywhere задачи не могут работать постоянно. Рассмотрите использование Always-on task или другого хостинга.

---

## ✈️ Fly.io

**Плюсы:** Бесплатный тариф, хорошая производительность

### Шаг 1: Установка Fly CLI

```bash
# Windows (PowerShell)
iwr https://fly.io/install.ps1 -useb | iex
```

### Шаг 2: Логин

```bash
fly auth login
```

### Шаг 3: Инициализация проекта

```bash
cd fitness-bot
fly launch
```

### Шаг 4: Настройка переменных окружения

```bash
fly secrets set TELEGRAM_BOT_TOKEN=your_token_here
```

### Шаг 5: Деплой

```bash
fly deploy
```

---

## 🖥️ VPS (DigitalOcean, AWS, Hetzner)

**Плюсы:** Полный контроль, можно использовать бесплатные варианты

### Шаг 1: Создание VPS

1. Создайте VPS на выбранном хостинге (Ubuntu 22.04)
2. Подключитесь по SSH:
   ```bash
   ssh root@your_server_ip
   ```

### Шаг 2: Установка зависимостей

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и pip
apt install python3 python3-pip python3-venv git -y
```

### Шаг 3: Клонирование проекта

```bash
git clone https://github.com/yourusername/fitness-bot.git
cd fitness-bot
```

### Шаг 4: Настройка виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Шаг 5: Настройка переменных окружения

```bash
nano .env
# Добавьте: TELEGRAM_BOT_TOKEN=your_token_here
```

### Шаг 6: Настройка systemd service

Создайте файл `/etc/systemd/system/fitness-bot.service`:

```ini
[Unit]
Description=Fitness Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/fitness-bot
Environment="PATH=/root/fitness-bot/venv/bin"
ExecStart=/root/fitness-bot/venv/bin/python -m app.main
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Шаг 7: Запуск сервиса

```bash
systemctl daemon-reload
systemctl enable fitness-bot
systemctl start fitness-bot
systemctl status fitness-bot
```

### Шаг 8: Просмотр логов

```bash
journalctl -u fitness-bot -f
```

---

## 🐳 Деплой через Docker

Если хостинг поддерживает Docker:

### Шаг 1: Создание .env файла

```bash
echo "TELEGRAM_BOT_TOKEN=your_token_here" > .env
```

### Шаг 2: Запуск через docker-compose

```bash
docker-compose up -d
```

### Шаг 3: Просмотр логов

```bash
docker-compose logs -f
```

---

## 📝 Общие рекомендации

### Безопасность

1. **Никогда не коммитьте `.env` файл в Git**
2. Используйте переменные окружения на хостинге
3. Регулярно обновляйте зависимости

### Мониторинг

1. Настройте логирование
2. Используйте мониторинг хостинга (если доступен)
3. Настройте уведомления об ошибках

### Резервное копирование

1. Регулярно делайте бэкап базы данных `fitness_bot.db`
2. Храните код в Git репозитории

---

## 🔧 Решение проблем

### Бот не запускается

1. Проверьте логи на хостинге
2. Убедитесь, что токен указан правильно
3. Проверьте, что все зависимости установлены

### Ошибки с базой данных

1. Убедитесь, что у приложения есть права на запись
2. Проверьте путь к базе данных в `config.py`

### Проблемы с памятью

1. На бесплатных тарифах может не хватать памяти
2. Рассмотрите использование более легких библиотек или платного тарифа

---

## 🎯 Рекомендация

**Для быстрого старта:** Railway или Render
- Простой деплой
- Бесплатный тариф
- Автоматические обновления

**Для продакшена:** VPS (DigitalOcean, Hetzner)
- Полный контроль
- Лучшая производительность
- Гибкость в настройке

---

**Удачного деплоя! 🚀**

