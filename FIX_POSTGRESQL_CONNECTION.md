# 🔧 Исправление подключения к PostgreSQL

## Проблема

В логах видно:
```
DATABASE_URL (raw): sqlite+aiosqlite:///data/fitness_bot.db
Database type: SQLite
```

Это означает, что бот **не использует PostgreSQL**, а все еще использует SQLite.

## Причина

Переменная `DATABASE_URL` **не установлена** в сервисе бота, или PostgreSQL сервис не создан/не запущен.

## Решение

### Шаг 1: Проверьте, создан ли PostgreSQL сервис

1. Откройте Railway Dashboard: https://railway.app
2. Выберите ваш проект
3. Проверьте список сервисов:
   - Должен быть сервис **PostgreSQL** (обычно с иконкой базы данных)
   - Должен быть сервис вашего бота (обычно называется "web" или имя вашего проекта)

**Если PostgreSQL сервиса нет:**
- Нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
- Дождитесь создания сервиса (обычно 1-2 минуты)

### Шаг 2: Проверьте переменную DATABASE_URL

1. В Railway Dashboard выберите ваш сервис бота (не PostgreSQL!)
2. Перейдите на вкладку **"Variables"**
3. Найдите переменную `DATABASE_URL`

**Если переменной `DATABASE_URL` нет:**
- Railway должен автоматически создать эту переменную при создании PostgreSQL
- Если переменной нет, добавьте ее вручную:
  1. В Railway Dashboard выберите сервис **PostgreSQL**
  2. Перейдите на вкладку **"Variables"**
  3. Найдите переменную `DATABASE_URL` (она должна быть там)
  4. Скопируйте значение `DATABASE_URL`
  5. Перейдите в сервис бота → **"Variables"**
  6. Нажмите **"+ New Variable"**
  7. Имя: `DATABASE_URL`
  8. Значение: вставьте скопированное значение
  9. Нажмите **"Add"**

**Или через Railway CLI:**
```bash
# Получите DATABASE_URL из PostgreSQL сервиса
railway service  # Выберите PostgreSQL
railway variables get DATABASE_URL

# Установите DATABASE_URL в сервисе бота
railway service  # Выберите сервис бота (web)
railway variables set DATABASE_URL="postgresql://..."
```

### Шаг 3: Перезапустите сервис бота

После установки переменной `DATABASE_URL`:

1. В Railway Dashboard выберите сервис бота
2. Перейдите на вкладку **"Settings"**
3. Нажмите **"Restart"**
4. Или просто сделайте новый деплой:
   ```bash
   git commit --allow-empty -m "Restart to use PostgreSQL"
   git push
   ```

### Шаг 4: Проверьте логи

После перезапуска проверьте логи:

```bash
railway logs
```

Должно быть:
```
DATABASE_URL provided: True
Using PostgreSQL database
Database type: PostgreSQL
✅ База данных инициализирована
```

Если все еще видите `Using SQLite on Railway`:
- Проверьте, что переменная `DATABASE_URL` установлена в сервисе бота
- Проверьте, что PostgreSQL сервис запущен
- Перезапустите сервис бота

## Автоматическая передача переменных в Railway

Railway **автоматически** передает переменные из PostgreSQL сервиса в другие сервисы, но иногда это не работает.

### Решение: Добавьте переменную вручную

1. **Получите DATABASE_URL из PostgreSQL сервиса:**
   - В Railway Dashboard выберите сервис PostgreSQL
   - Перейдите на вкладку **"Variables"**
   - Найдите переменную `DATABASE_URL`
   - Скопируйте значение

2. **Добавьте DATABASE_URL в сервис бота:**
   - В Railway Dashboard выберите сервис бота
   - Перейдите на вкладку **"Variables"**
   - Нажмите **"+ New Variable"**
   - Имя: `DATABASE_URL`
   - Значение: вставьте скопированное значение
   - Нажмите **"Add"**

3. **Перезапустите сервис бота**

## Проверка через Railway CLI

```bash
# Проверьте переменные в сервисе бота
railway service  # Выберите сервис бота
railway variables

# Должна быть переменная DATABASE_URL
# Если ее нет, добавьте:
railway variables set DATABASE_URL="postgresql://..."
```

## Альтернативное решение: Используйте Railway Service Variables

Railway имеет функцию **Service Variables**, которая автоматически передает переменные между сервисами.

1. В Railway Dashboard выберите ваш проект
2. Перейдите на вкладку **"Variables"** (на уровне проекта, не сервиса)
3. Проверьте, есть ли там `DATABASE_URL`
4. Если есть, она должна быть доступна всем сервисам в проекте

## После исправления

После установки переменной `DATABASE_URL` и перезапуска:

1. **Проверьте логи:**
   ```bash
   railway logs
   ```
   Должно быть: `Using PostgreSQL database`

2. **Проверьте таблицы:**
   ```bash
   railway connect postgres
   \dt
   ```

3. **Проверьте работу бота:**
   - Откройте бота в Telegram
   - Добавьте тестовую программу
   - Проверьте, что программа сохранилась в PostgreSQL

## Чек-лист

- [ ] PostgreSQL сервис создан в Railway
- [ ] PostgreSQL сервис запущен
- [ ] Переменная `DATABASE_URL` установлена в сервисе бота
- [ ] Сервис бота перезапущен
- [ ] В логах: `Using PostgreSQL database`
- [ ] Бот работает и сохраняет данные в PostgreSQL

## ❓ Часто задаваемые вопросы

### Почему Railway не передает DATABASE_URL автоматически?

Иногда Railway не передает переменные автоматически между сервисами. В этом случае нужно добавить переменную вручную.

### Как проверить, что PostgreSQL работает?

```bash
# Подключитесь к PostgreSQL
railway connect postgres

# Проверьте версию
SELECT version();

# Проверьте таблицы
\dt
```

### Что делать, если переменная DATABASE_URL не работает?

1. Проверьте формат переменной: `postgresql://user:password@host:port/database`
2. Убедитесь, что PostgreSQL сервис запущен
3. Проверьте, что переменная установлена в правильном сервисе (бот, а не PostgreSQL)
4. Перезапустите сервис бота

---

**После выполнения этих шагов бот должен использовать PostgreSQL!** 🎉

