# 📥 Установка Git для Windows

## Вариант 1: Установка через установщик (рекомендуется)

1. **Скачайте Git:**
   - Перейдите на [git-scm.com/download/win](https://git-scm.com/download/win)
   - Скачайте установщик (автоматически определит 64-bit версию)

2. **Установите Git:**
   - Запустите скачанный файл
   - Нажимайте "Next" на всех шагах (настройки по умолчанию подойдут)
   - В конце нажмите "Install"

3. **Проверьте установку:**
   - Закройте и откройте PowerShell/терминал заново
   - Выполните: `git --version`
   - Должна появиться версия Git

## Вариант 2: Установка через winget (если доступен)

```powershell
winget install --id Git.Git -e --source winget
```

## Вариант 3: Установка через Chocolatey (если установлен)

```powershell
choco install git
```

---

## После установки Git:

1. **Перезапустите PowerShell/терминал**

2. **Настройте Git (первый раз):**
   ```bash
   git config --global user.name "Ваше Имя"
   git config --global user.email "your.email@example.com"
   ```

3. **Теперь можно использовать команды:**
   ```bash
   cd C:\Users\Roman\fitness-bot
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/winslods-afk/fitness-bot.git
   git push -u origin main
   ```

---

## Альтернатива: Загрузка через GitHub Desktop

Если не хотите устанавливать Git через командную строку:

1. **Скачайте GitHub Desktop:**
   - [desktop.github.com](https://desktop.github.com)

2. **Установите и войдите в GitHub**

3. **Создайте репозиторий:**
   - File → New Repository
   - Name: `fitness-bot`
   - Local Path: `C:\Users\Roman\fitness-bot`
   - Нажмите "Create Repository"

4. **Загрузите код:**
   - Нажмите "Publish repository"
   - Выберите "Keep this code private" (опционально)
   - Нажмите "Publish repository"

---

## Быстрая установка (самый простой способ):

1. Откройте браузер
2. Перейдите на: https://git-scm.com/download/win
3. Скачайте и установите (просто нажимайте Next)
4. Перезапустите PowerShell
5. Готово!

