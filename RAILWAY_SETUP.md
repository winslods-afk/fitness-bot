# 🚀 Установка Railway CLI - Пошаговая инструкция

## 📍 Где выполнять команды

Все команды нужно выполнять в **терминале** (PowerShell или Command Prompt).

---

## 🔧 Шаг 1: Откройте терминал

### Вариант A: PowerShell (рекомендуется для Windows)

1. Нажмите `Win + X` (или правой кнопкой на меню Пуск)
2. Выберите **Windows PowerShell** или **Терминал**
3. Или нажмите `Win + R`, введите `powershell` и нажмите Enter

### Вариант B: Command Prompt

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter

### Вариант C: Через VS Code / Cursor

1. Откройте VS Code или Cursor
2. Нажмите `` Ctrl + ` `` (обратная кавычка) для открытия терминала
3. Или меню: **Terminal** → **New Terminal**

---

## ✅ Шаг 2: Проверьте, установлен ли Node.js и npm

Перед установкой Railway CLI нужно убедиться, что у вас установлен Node.js (который включает npm).

Выполните в терминале:

```powershell
node --version
npm --version
```

**Если команды не найдены:**

1. **Скачайте Node.js:**
   - Перейдите на https://nodejs.org/
   - Скачайте LTS версию (рекомендуется)
   - Установите (в процессе установки выберите "Add to PATH")

2. **Перезапустите терминал** после установки

3. **Проверьте снова:**
   ```powershell
   node --version
   npm --version
   ```

---

## 📦 Шаг 3: Установите Railway CLI

Выполните в терминале:

```powershell
npm i -g @railway/cli
```

**Что делает эта команда:**
- `npm` — менеджер пакетов Node.js
- `i` — сокращение от `install` (установить)
- `-g` — глобальная установка (доступна из любой папки)
- `@railway/cli` — пакет Railway CLI

**Ожидаемый вывод:**
```
added 1 package, and audited 2 packages in 3s
```

---

## ✅ Шаг 4: Проверьте установку

Выполните:

```powershell
railway --version
```

**Ожидаемый вывод:**
```
railway/2.x.x
```

Если команда не найдена:
- Перезапустите терминал
- Убедитесь, что Node.js установлен правильно
- Попробуйте установить снова

---

## 🔐 Шаг 5: Войдите в Railway

```powershell
railway login
```

Откроется браузер для авторизации. Войдите в свой аккаунт Railway.

---

## 🔗 Шаг 6: Подключите проект

```powershell
cd C:\Users\Roman\fitness-bot
railway link
```

Выберите ваш проект из списка.

---

## 📥 Шаг 7: Синхронизируйте базу данных

```powershell
python sync_db.py
```

---

## ❓ Решение проблем

### Ошибка: "npm не является внутренней или внешней командой"

**Решение:** Установите Node.js с https://nodejs.org/

### Ошибка: "EACCES: permission denied"

**Решение (Windows):** Запустите PowerShell от имени администратора:
1. Правой кнопкой на PowerShell
2. Выберите "Запуск от имени администратора"
3. Выполните команду снова

### Ошибка: "railway: command not found"

**Решение:**
1. Перезапустите терминал
2. Проверьте, что npm установлен: `npm --version`
3. Попробуйте установить снова: `npm i -g @railway/cli`

### Ошибка при `railway login`

**Решение:**
- Убедитесь, что у вас есть аккаунт на https://railway.app
- Попробуйте открыть браузер вручную и войти
- Затем выполните `railway login` снова

---

## 📚 Дополнительная информация

- **Официальная документация Railway CLI:** https://docs.railway.app/develop/cli
- **Node.js документация:** https://nodejs.org/

---

## ✅ Быстрая проверка

Выполните все команды по порядку:

```powershell
# 1. Проверка Node.js
node --version
npm --version

# 2. Установка Railway CLI
npm i -g @railway/cli

# 3. Проверка Railway CLI
railway --version

# 4. Вход в Railway
railway login

# 5. Переход в папку проекта
cd C:\Users\Roman\fitness-bot

# 6. Подключение проекта
railway link

# 7. Синхронизация БД
python sync_db.py
```

Если все команды выполнились успешно — готово! 🎉

