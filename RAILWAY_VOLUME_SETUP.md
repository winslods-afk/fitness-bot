# 💾 Настройка персистентного хранилища для базы данных на Railway

## Проблема

При каждом деплое база данных удаляется, потому что она хранится в файловой системе контейнера, которая очищается при пересборке.

## Решение: Railway Volume

Railway предоставляет **Volumes** - персистентное хранилище, которое сохраняется между деплоями.

## 📋 Пошаговая инструкция

### Шаг 1: Создайте Volume в Railway

1. Откройте ваш проект в [Railway Dashboard](https://railway.app/)
2. Перейдите в раздел вашего сервиса (service)
3. Нажмите на вкладку **"Volumes"** (или **"+ New"** → **"Volume"**)
4. Создайте новый Volume:
   - **Name**: `database-storage` (или любое другое имя)
   - **Mount Path**: `/data`
   - Нажмите **"Add"**

### Шаг 2: Проверьте переменные окружения

Убедитесь, что в **Settings → Variables** установлено:

```
DATABASE_PATH=/data/fitness_bot.db
```

Или оставьте по умолчанию - код автоматически определит Railway окружение.

### Шаг 3: Перезапустите сервис

После создания Volume:
1. Перейдите в **Deployments**
2. Нажмите **"Redeploy"** или дождитесь автоматического деплоя

### Шаг 4: Проверьте

После деплоя база данных будет храниться в `/data/fitness_bot.db` внутри Volume, который сохраняется между деплоями.

## 🔍 Проверка через Railway CLI

Вы можете проверить наличие Volume через CLI:

```bash
railway volume
```

Или проверить содержимое:

```bash
railway run ls -la /data
```

## 📝 Альтернативные решения

### Вариант 1: Использовать внешнюю БД (PostgreSQL)

Если нужна более надежная БД, можно использовать Railway PostgreSQL:

1. В Railway Dashboard нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст переменную `DATABASE_URL`
3. Обновите код для использования PostgreSQL вместо SQLite

### Вариант 2: Резервное копирование

Настройте автоматическое резервное копирование базы данных:

1. Используйте команду `/export_db` в боте для экспорта
2. Настройте cron-задачу для регулярного экспорта
3. Сохраняйте бэкапы в облачное хранилище (S3, Google Drive и т.д.)

## ⚠️ Важные замечания

1. **Volume создается один раз** - после создания он сохраняется между деплоями
2. **Размер Volume** - Railway предоставляет ограниченный объем бесплатно
3. **Резервное копирование** - рекомендуется делать регулярные бэкапы даже с Volume

## 🛠️ Устранение неполадок

### База данных все еще удаляется

1. Проверьте, что Volume создан и смонтирован в `/data`
2. Проверьте логи деплоя - должна быть строка о создании директории `/data`
3. Убедитесь, что `DATABASE_PATH` указывает на `/data/fitness_bot.db`

### Ошибка "Permission denied"

Если возникает ошибка доступа:
```bash
railway run chmod 777 /data
```

### Проверка содержимого Volume

```bash
railway run ls -la /data
railway run cat /data/fitness_bot.db | head -c 100
```

## 📚 Дополнительная информация

- [Railway Volumes Documentation](https://docs.railway.app/develop/volumes)
- [Railway Persistence Guide](https://docs.railway.app/develop/persistent-storage)

