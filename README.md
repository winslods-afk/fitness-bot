# Telegram-бот "Фитнес" (отслеживание тренировок и рабочих весов)

Полнофункциональный Telegram-бот для создания программ тренировок и отслеживания рабочих весов. Разработан на Python с использованием aiogram 3.x и асинхронной работы с базой данных SQLite.

## 🎯 Основные возможности

- ✅ Создание программ тренировок (до 2 программ на пользователя)
- ✅ Гибкий парсинг упражнений в различных форматах
- ✅ Проведение тренировок с отслеживанием весов
- ✅ Сравнение текущих результатов с предыдущими тренировками
- ✅ Удаление программ с подтверждением
- ✅ Удобный интерфейс с клавиатурами

## 📋 Требования

- Python 3.11+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и укажите токен бота:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

### 3. Запуск бота

```bash
python -m app.main
```

Или напрямую:

```bash
python app/main.py
```

## 🐳 Запуск в Docker

### Сборка и запуск:

```bash
docker-compose up -d
```

### Просмотр логов:

```bash
docker-compose logs -f
```

### Остановка:

```bash
docker-compose down
```

## 📁 Структура проекта

```
.
├── app/
│   ├── __init__.py
│   ├── main.py              # Точка входа
│   ├── bot.py               # Инициализация бота
│   ├── config.py            # Конфигурация
│   ├── db/
│   │   ├── __init__.py
│   │   ├── models.py        # Модели БД (SQLAlchemy)
│   │   ├── crud.py          # CRUD операции
│   │   └── init_db.py       # Инициализация БД
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── start.py         # Команды /start, /help, /myprograms
│   │   ├── add_program.py   # Добавление программы
│   │   ├── delete_program.py # Удаление программы
│   │   └── training.py      # Проведение тренировки
│   ├── services/
│   │   ├── __init__.py
│   │   ├── parser.py        # Парсер упражнений
│   │   └── stats.py         # Статистика и сравнение
│   └── utils/
│       ├── __init__.py
│       ├── keyboards.py     # Клавиатуры
│       └── messages.py      # Шаблоны сообщений
├── requirements.txt
├── .env.example
├── Dockerfile
├── docker-compose.yml
└── README.md
```

## 🗄️ База данных

Проект использует **SQLAlchemy с асинхронным движком** (aiosqlite) для работы с SQLite.

### Обоснование выбора

**SQLAlchemy async** выбран по следующим причинам:
- ✅ Зрелая и стабильная библиотека с отличной документацией
- ✅ Хорошая интеграция с aiogram (асинхронный стиль)
- ✅ Гибкость в работе с различными БД (легко перейти на PostgreSQL)
- ✅ Автоматическое создание таблиц при старте
- ✅ Поддержка миграций через Alembic (при необходимости)

**Альтернатива (Tortoise ORM)** не выбрана, так как:
- Менее популярна и имеет меньшую экосистему
- SQLAlchemy более универсальна и широко используется

### Структура БД

- **users** — пользователи бота
- **sessions** — программы тренировок
- **workout_days** — дни программы
- **exercises** — упражнения
- **sets** — подходы (шаблоны)
- **session_runs** — запуски тренировок
- **performed_sets** — выполненные подходы с весами

База данных создаётся автоматически при первом запуске в файле `fitness_bot.db`.

### Просмотр данных

Для просмотра данных можно использовать:
- [DB Browser for SQLite](https://sqlitebrowser.org/)
- Командная строка: `sqlite3 fitness_bot.db`

## 📝 Использование бота

### Команды

- `/start` — начать работу с ботом
- `/restart` — перезапустить бота (сброс состояний)
- `/help` — справка по использованию
- `/myprograms` — показать все ваши программы

### Добавление программы

1. Нажмите "Добавить программу"
2. Выберите количество тренировочных дней (1-7)
3. Для каждого дня введите название (например: "Спина", "Грудь")
4. Добавьте упражнения в одном из форматов:
   - `Гакк-присед — 20-16-14-12`
   - `Жим лёжа — 4х10`
   - `Подтягивания — 4 подхода по 10 раз`
5. Отправьте `/done` после завершения упражнений для дня
6. Введите название программы

### Парсинг упражнений

Бот поддерживает следующие форматы ввода:

1. **Список повторений**: `Упражнение — 20-16-14-12`
   - Создаёт 4 подхода: 20, 16, 14, 12 повторений

2. **Количество подходов × повторения**: `Упражнение — 4х10` или `Упражнение — 4x10`
   - Создаёт 4 подхода по 10 повторений

3. **Текстовый формат**: `Упражнение — 4 подхода по 10 раз`
   - Создаёт 4 подхода по 10 повторений

4. **Комбинированный**: `Упражнение — 4x10, 3x8`
   - Создаёт 4 подхода по 10 и 3 подхода по 8 повторений

### Проведение тренировки

1. Нажмите "Начать тренировку"
2. Выберите программу (если их несколько)
3. Выберите тренировочный день
4. Просмотрите список упражнений и нажмите "Начать"
5. Для каждого подхода введите вес (в кг)
6. После завершения получите итоговую сводку с сравнением

### Удаление программы

1. Нажмите "Удалить программу"
2. Выберите программу из списка
3. Подтвердите удаление

## 🔧 Технические детали

### Парсинг упражнений

Парсер использует регулярные выражения для распознавания различных форматов:

```python
# Формат "20-16-14-12"
r'^\d+(-\d+)+$'

# Формат "4x10" или "4х10"
r'^\d+[xх]\d+$'

# Формат "4 подхода по 10 раз"
r'(\d+)\s+подход[а-я]*\s+по\s+(\d+)'
```

Подробности реализации в `app/services/parser.py`.

### FSM (Finite State Machine)

Бот использует FSM для управления состояниями:
- **AddProgramStates** — для добавления программы
- **TrainingStates** — для проведения тренировки

### Middleware

Реализован middleware для автоматической работы с сессиями БД в каждом обработчике.

## 🧪 Тестирование

### Пример использования

1. Запустите бота
2. Отправьте `/start`
3. Добавьте программу:
   - Выберите 3 дня
   - День 1: "Спина"
     - `Подтягивания — 10-8-6`
     - `Тяга штанги — 4х8`
   - День 2: "Грудь"
     - `Жим лёжа — 4 подхода по 10 раз`
   - День 3: "Ноги"
     - `Приседания — 12-10-8-6`
4. Название программы: "Моя программа"
5. Начните тренировку и введите веса

### Проверка данных

```bash
sqlite3 fitness_bot.db
.tables
SELECT * FROM users;
SELECT * FROM sessions;
```

## 📦 Зависимости

- `aiogram==3.13.1` — фреймворк для Telegram-ботов
- `aiosqlite==0.20.0` — асинхронный драйвер для SQLite
- `sqlalchemy[asyncio]==2.0.36` — ORM с поддержкой async
- `python-dotenv==1.0.1` — загрузка переменных окружения

## 🐛 Решение проблем

### Бот не отвечает

- Проверьте, что токен указан правильно в `.env`
- Убедитесь, что бот запущен и нет ошибок в логах
- Проверьте интернет-соединение

### Ошибки базы данных

- Удалите файл `fitness_bot.db` и перезапустите бота (БД создастся заново)
- Проверьте права доступа к файлу БД

### Проблемы с парсингом упражнений

- Используйте один из поддерживаемых форматов
- Убедитесь, что между названием и подходами есть разделитель (—, -, –)

## 📄 Лицензия

Проект создан в образовательных целях.

## 👤 Автор

Разработано с использованием aiogram 3.x и SQLAlchemy async.

---

**Приятных тренировок! 💪**

