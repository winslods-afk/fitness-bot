# Проверка работы Volume

## ✅ Volume создан!

Теперь база данных будет сохраняться между деплоями.

## Что происходит сейчас:

1. **Volume создан и привязан к сервису `web`**
   - Mount path: `/data`
   - База данных будет храниться в `/data/fitness_bot.db`

2. **Конфигурация приложения**
   - `app/config.py` автоматически определяет Railway окружение
   - Использует путь `/data/fitness_bot.db` для базы данных
   - Создает директорию `/data` при старте (если не существует)

3. **Конфигурация Railway**
   - `railway.json` содержит настройки volume
   - Railway автоматически монтирует volume при каждом деплое

## Как проверить, что все работает:

### 1. Проверьте логи после деплоя

```bash
railway logs
```

Должно быть сообщение:
```
База данных инициализирована
```

### 2. Протестируйте сохранение данных

1. **Добавьте тестовую программу через бота:**
   - Нажмите "Мои Программы тренировок"
   - Выберите "Добавить программу"
   - Создайте простую программу (например, 1 день, 1 упражнение)

2. **Сделайте новый деплой:**
   - Измените что-то в коде (например, README.md)
   - Закоммитьте и запушьте:
     ```bash
     git add README.md
     git commit -m "Test deploy"
     git push
     ```

3. **Проверьте, что программа осталась:**
   - После деплоя откройте бота
   - Нажмите "Мои Программы тренировок"
   - Выберите "Удалить программу" (чтобы увидеть список)
   - Программа должна быть там!

### 3. Проверьте базу данных (опционально)

Если хотите посмотреть базу данных напрямую:

```bash
# Скачайте базу данных
railway run bash
# Или используйте команду бота /export_db (если у вас есть права администратора)
```

## Что изменилось:

### До настройки volume:
- ❌ При каждом деплое база данных удалялась
- ❌ Все программы и данные терялись
- ❌ Нужно было заново добавлять программы

### После настройки volume:
- ✅ База данных сохраняется между деплоями
- ✅ Все программы и данные остаются
- ✅ Можно безопасно делать деплои

## Важные замечания:

1. **Volume сохраняет данные между деплоями**
   - Данные не теряются при обновлении кода
   - Данные не теряются при перезапуске сервиса

2. **Резервное копирование**
   - Используйте команду `/export_db` для скачивания базы данных
   - Регулярно делайте резервные копии важных данных

3. **Если нужно удалить volume:**
   ```bash
   railway volume delete <volume-name>
   ```
   ⚠️ **ВНИМАНИЕ:** Это удалит все данные! Используйте только если уверены.

4. **Если нужно посмотреть использование volume:**
   ```bash
   railway volume list
   ```
   Покажет объем использованного хранилища

## Устранение проблем:

### База данных все еще сбрасывается

1. Проверьте, что volume привязан:
   ```bash
   railway volume list
   ```
   Должен быть volume с mount path `/data`

2. Проверьте логи:
   ```bash
   railway logs
   ```
   Должно быть: `База данных инициализирована`

3. Проверьте переменные окружения:
   - Railway автоматически устанавливает `RAILWAY_ENVIRONMENT` или `RAILWAY`
   - Приложение должно определять это и использовать `/data`

### Ошибка "Permission denied" при создании БД

Если появляется ошибка доступа к `/data`:
- Проверьте, что volume правильно смонтирован
- Перезапустите сервис через Railway Dashboard
- Проверьте логи на наличие ошибок

## Готово! 🎉

Теперь ваша база данных будет сохраняться между деплоями. Можете спокойно обновлять код - данные пользователей не потеряются!

