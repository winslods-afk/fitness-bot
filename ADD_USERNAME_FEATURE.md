# ✅ Добавлено сохранение Telegram username

## Что изменилось

Теперь бот сохраняет **Telegram username** пользователей в базе данных.

## Изменения в коде

### 1. Модель User

Добавлено поле `username` в модель `User`:
- Тип: `String` (VARCHAR в PostgreSQL)
- Может быть `NULL` (не у всех пользователей есть username)
- Индексировано для быстрого поиска

### 2. Функция get_or_create_user

Функция `get_or_create_user` теперь:
- Принимает параметр `username` (опционально)
- Сохраняет username при создании нового пользователя
- **Автоматически обновляет username**, если он изменился (username может меняться)

### 3. Все обработчики обновлены

Все обработчики теперь передают `username` при создании/получении пользователя:
- `start.py` - команды /start, /myprograms
- `add_program.py` - добавление программ
- `delete_program.py` - удаление программ
- `training.py` - проведение тренировок
- `stats.py` - просмотр статистики
- `ai_program.py` - создание программ через AI

## Миграция базы данных

### Для новой установки

Если база данных создается заново:
- ✅ Поле `username` будет создано автоматически
- ✅ Никаких действий не требуется

### Для существующей базы данных

Если у вас уже есть база данных с пользователями:

#### Вариант 1: Через DBeaver (рекомендуется)

1. **Откройте DBeaver и подключитесь к PostgreSQL**
2. **Откройте SQL Editor**
3. **Выполните SQL запрос:**

```sql
-- Добавляем колонку username
ALTER TABLE users ADD COLUMN IF NOT EXISTS username VARCHAR;

-- Создаем индекс для быстрого поиска
CREATE INDEX IF NOT EXISTS ix_users_username ON users(username);
```

4. **Проверьте результат:**
```sql
SELECT column_name, data_type, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'users' AND table_schema = 'public';
```

Должно быть:
- `id` - integer
- `telegram_id` - integer
- `username` - character varying ✅ (новое поле)
- `created_at` - timestamp without time zone

#### Вариант 2: Через Railway CLI

```bash
# Подключитесь к PostgreSQL
railway connect postgres

# Выполните SQL запросы
ALTER TABLE users ADD COLUMN IF NOT EXISTS username VARCHAR;
CREATE INDEX IF NOT EXISTS ix_users_username ON users(username);
```

#### Вариант 3: Используйте файл миграции

1. **Откройте файл `migrate_add_username.sql`**
2. **Скопируйте SQL запросы**
3. **Выполните в DBeaver или через Railway CLI**

## Проверка работы

### 1. Проверьте структуру таблицы

```sql
SELECT column_name, data_type, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'users' AND table_schema = 'public'
ORDER BY ordinal_position;
```

### 2. Проверьте данные пользователей

```sql
SELECT id, telegram_id, username, created_at 
FROM users 
ORDER BY created_at DESC;
```

### 3. Проверьте работу бота

1. **Откройте бота в Telegram**
2. **Отправьте `/start`**
3. **Добавьте программу или начните тренировку**
4. **Проверьте в базе данных:**
   ```sql
   SELECT telegram_id, username 
   FROM users 
   WHERE telegram_id = YOUR_TELEGRAM_ID;
   ```

Должен сохраниться ваш username (если он указан в Telegram).

## Что происходит с существующими пользователями?

- ✅ **Существующие пользователи** - поле `username` будет `NULL` до первого взаимодействия с ботом
- ✅ **При первом взаимодействии** - username автоматически сохранится
- ✅ **При изменении username** - username автоматически обновится при следующем взаимодействии

## Использование username

Теперь вы можете:

### 1. Искать пользователей по username

```sql
SELECT * FROM users WHERE username = 'username_here';
```

### 2. Просматривать пользователей с username

```sql
SELECT telegram_id, username, created_at 
FROM users 
WHERE username IS NOT NULL
ORDER BY created_at DESC;
```

### 3. Просматривать пользователей без username

```sql
SELECT telegram_id, username, created_at 
FROM users 
WHERE username IS NULL
ORDER BY created_at DESC;
```

### 4. Использовать в статистике

```sql
SELECT 
    u.telegram_id,
    u.username,
    COUNT(DISTINCT s.session_id) as programs_count,
    COUNT(DISTINCT sr.id) as workouts_count
FROM users u
LEFT JOIN sessions s ON u.id = s.user_id
LEFT JOIN session_runs sr ON s.session_id = sr.session_id
GROUP BY u.id, u.telegram_id, u.username
ORDER BY workouts_count DESC;
```

## Важные замечания

1. **Username может быть NULL:**
   - Не все пользователи Telegram указывают username
   - Это нормально, поле `username` может быть `NULL`

2. **Username может меняться:**
   - Пользователи могут менять username в Telegram
   - Бот автоматически обновляет username при каждом взаимодействии

3. **Username не уникален:**
   - Пользователи могут иметь одинаковый username (редко, но возможно)
   - `telegram_id` остается уникальным идентификатором

4. **Индекс на username:**
   - Создан индекс для быстрого поиска по username
   - Это ускоряет запросы с фильтром по username

## Откат миграции (если нужно)

Если нужно удалить поле `username`:

```sql
-- Удаляем индекс
DROP INDEX IF EXISTS ix_users_username;

-- Удаляем колонку
ALTER TABLE users DROP COLUMN IF EXISTS username;
```

**⚠️ Внимание:** Это удалит все сохраненные username!

## После миграции

После выполнения миграции:

1. **Перезапустите бота** (если нужно)
2. **Проверьте логи** - не должно быть ошибок
3. **Проверьте работу бота** - создайте программу или начните тренировку
4. **Проверьте базу данных** - username должен сохраниться

## SQL запросы для проверки

### Просмотр всех пользователей с username:

```sql
SELECT 
    u.id,
    u.telegram_id,
    u.username,
    u.created_at,
    COUNT(DISTINCT s.session_id) as programs_count
FROM users u
LEFT JOIN sessions s ON u.id = s.user_id
GROUP BY u.id, u.telegram_id, u.username, u.created_at
ORDER BY u.created_at DESC;
```

### Поиск пользователя по username:

```sql
SELECT * FROM users WHERE username = 'your_username';
```

### Статистика по username:

```sql
SELECT 
    CASE 
        WHEN username IS NULL THEN 'Без username'
        ELSE 'С username'
    END as user_type,
    COUNT(*) as count
FROM users
GROUP BY user_type;
```

---

**Готово!** Теперь бот сохраняет Telegram username пользователей! 🎉

