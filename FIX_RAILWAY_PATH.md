# 🔧 Исправление: Railway CLI не найден

Если вы получили ошибку `railway : Имя "railway" не распознано`, выполните одно из решений ниже.

## ✅ Решение 1: Перезапустите терминал (самый простой)

1. **Закройте текущий терминал** (PowerShell или Command Prompt)
2. **Откройте новый терминал**
3. Выполните команду снова:
   ```powershell
   railway --version
   ```

Windows автоматически обновит PATH при перезапуске терминала.

---

## ✅ Решение 2: Обновите PATH в текущей сессии

Выполните в текущем терминале:

```powershell
$env:Path += ";C:\Users\Roman\AppData\Roaming\npm"
railway --version
```

**Недостаток:** PATH обновится только для текущей сессии. После закрытия терминала нужно будет выполнить команду снова.

---

## ✅ Решение 3: Добавьте путь в PATH постоянно

### Через PowerShell (от имени администратора):

1. Откройте PowerShell **от имени администратора**
2. Выполните:
   ```powershell
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\Users\Roman\AppData\Roaming\npm", "User")
   ```
3. Перезапустите терминал

### Через графический интерфейс:

1. Нажмите `Win + R`, введите `sysdm.cpl` и нажмите Enter
2. Перейдите на вкладку **Дополнительно**
3. Нажмите **Переменные среды**
4. В разделе **Переменные пользователя** найдите `Path` и нажмите **Изменить**
5. Нажмите **Создать** и добавьте: `C:\Users\Roman\AppData\Roaming\npm`
6. Нажмите **ОК** во всех окнах
7. **Перезапустите терминал**

---

## 🔍 Проверка установки

После применения любого из решений выполните:

```powershell
railway --version
```

**Ожидаемый вывод:**
```
railway 4.11.0
```

---

## 📝 Быстрая команда для текущей сессии

Если вы не хотите перезапускать терминал, выполните перед каждой командой `railway`:

```powershell
$env:Path += ";C:\Users\Roman\AppData\Roaming\npm"
```

Или создайте алиас в PowerShell:

```powershell
# Добавьте в профиль PowerShell (выполните один раз)
Add-Content $PROFILE "`n`$env:Path += `";C:\Users\Roman\AppData\Roaming\npm`""

# Затем перезагрузите профиль
. $PROFILE
```

---

## ⚠️ Если ничего не помогает

1. Переустановите Railway CLI:
   ```powershell
   npm uninstall -g @railway/cli
   npm install -g @railway/cli
   ```

2. Проверьте, что npm работает:
   ```powershell
   npm --version
   ```

3. Найдите, где установлен Railway:
   ```powershell
   npm list -g @railway/cli
   where.exe railway
   ```

4. Добавьте найденный путь в PATH (см. Решение 3)

---

**Рекомендация:** Используйте **Решение 1** (перезапуск терминала) — это самый простой и надежный способ.

