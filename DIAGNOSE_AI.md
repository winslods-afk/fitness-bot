# 🔍 Диагностика AI ассистента

## Быстрая проверка

Запустите скрипт диагностики:

```powershell
cd C:\Users\Roman\fitness-bot
python check_ai.py
```

Или для Railway (после синхронизации базы):
```powershell
.\sync_db.ps1
python check_ai.py
```

## Возможные проблемы

### 1. AI_PROVIDER не установлен

**Симптом:** Бот отвечает "AI ассистент не настроен"

**Решение:**
- Установите переменную окружения `AI_PROVIDER`
- Варианты: `openai`, `yandex`, `anthropic`, `google`, `groq`, `ollama`

**Для Railway:**
1. Откройте проект в Railway Dashboard
2. Перейдите в Settings → Variables
3. Добавьте: `AI_PROVIDER=groq` (или другой провайдер)

**Для локальной разработки:**
Создайте файл `.env`:
```
AI_PROVIDER=groq
GROQ_API_KEY=your_key_here
```

### 2. API ключ не установлен

**Симптом:** Бот отвечает "AI ассистент временно недоступен"

**Решение:**
Установите соответствующий API ключ:

| Провайдер | Переменная окружения |
|-----------|---------------------|
| OpenAI | `OPENAI_API_KEY` |
| Yandex | `YANDEX_API_KEY` и `YANDEX_FOLDER_ID` |
| Anthropic | `ANTHROPIC_API_KEY` |
| Google | `GOOGLE_API_KEY` |
| Groq | `GROQ_API_KEY` |
| Ollama | `OLLAMA_URL` (по умолчанию `http://localhost:11434`) |

### 3. Неверный API ключ

**Симптом:** Бот отвечает "не удалось получить ответ от AI ассистента"

**Решение:**
- Проверьте правильность API ключа
- Убедитесь, что ключ активен и не истек
- Проверьте лимиты использования API

### 4. Проблемы с сетью/API

**Симптом:** Ошибки в логах, AI не отвечает

**Решение:**
- Проверьте подключение к интернету
- Проверьте статус API провайдера
- Попробуйте другой провайдер (например, Groq - быстрый и бесплатный)

## Рекомендуемая настройка (Groq)

Groq - быстрый и бесплатный вариант:

1. **Получите API ключ:**
   - Зарегистрируйтесь на https://console.groq.com/
   - Создайте API ключ

2. **Установите переменные в Railway:**
   ```
   AI_PROVIDER=groq
   GROQ_API_KEY=gsk_your_key_here
   ```

3. **Перезапустите бота**

## Проверка логов

Для просмотра логов на Railway:

1. Откройте проект в Railway Dashboard
2. Перейдите в Deployments
3. Выберите последний деплой
4. Откройте вкладку "Logs"

Ищите строки с:
- `AI error` - ошибки AI
- `AI handler error` - ошибки обработчика

## Тестирование

После настройки отправьте боту любое сообщение (не команду и не кнопку меню), например:
```
Как правильно делать приседания?
```

Бот должен ответить через AI.

## Структура проверки

Код проверяет AI в следующем порядке:

1. `is_ai_enabled()` - проверяет, что `AI_PROVIDER != "none"` и есть API ключ
2. `get_ai_response()` - пытается получить ответ от выбранного провайдера
3. Если ошибка - логируется и показывается пользователю понятное сообщение

## Дополнительная информация

См. также:
- `AI_SETUP.md` - подробная инструкция по настройке всех провайдеров
- `check_ai.py` - скрипт диагностики

