# 🤖 Настройка AI ассистента

Бот поддерживает интеграцию с AI для ответов на свободные сообщения пользователей.

## 🎯 Поддерживаемые провайдеры

1. **OpenAI GPT** (GPT-3.5, GPT-4)
2. **Yandex GPT** (YandexGPT)
3. **Anthropic Claude** (Claude 3)
4. **Отключено** (по умолчанию)

## ⚙️ Настройка

### Шаг 1: Выберите провайдера

В файле `.env` добавьте:

```env
AI_PROVIDER=openai  # или yandex, anthropic, none
```

### Шаг 2: Настройте API ключи

#### OpenAI

1. Получите API ключ: [platform.openai.com/api-keys](https://platform.openai.com/api-keys)

2. Добавьте в `.env`:
```env
AI_PROVIDER=openai
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-3.5-turbo  # или gpt-4, gpt-4-turbo
```

#### Yandex GPT

1. Создайте сервисный аккаунт в [Yandex Cloud](https://cloud.yandex.ru/)
2. Получите API ключ и Folder ID
3. Добавьте в `.env`:
```env
AI_PROVIDER=yandex
YANDEX_API_KEY=AQVN...
YANDEX_FOLDER_ID=b1g...
```

#### Anthropic Claude

1. Получите API ключ: [console.anthropic.com](https://console.anthropic.com/)
2. Добавьте в `.env`:
```env
AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-3-haiku-20240307  # или claude-3-sonnet, claude-3-opus
```

### Шаг 3: Установите зависимости

```bash
pip install -r requirements.txt
```

Или установите только нужный пакет:

```bash
# Для OpenAI
pip install openai

# Для Yandex (aiohttp уже в requirements.txt)
pip install aiohttp

# Для Anthropic
pip install anthropic
```

### Шаг 4: Перезапустите бота

```bash
python -m app.main
```

## 🎨 Как это работает

1. Пользователь отправляет свободное сообщение (не команду)
2. Бот проверяет, что пользователь не в процессе (добавление программы, тренировка)
3. Бот отправляет запрос к AI с контекстом о фитнес-боте
4. AI генерирует ответ на основе контекста
5. Бот отправляет ответ пользователю

## 📝 Контекст для AI

AI получает следующий контекст:

- Описание бота (фитнес-бот для отслеживания тренировок)
- Задачи AI (помощь с тренировками, питанием, техникой)
- Информация о программах пользователя (если есть)

## 💡 Примеры использования

**Пользователь:** "Как правильно делать жим лёжа?"
**AI:** "💪 Жим лёжа — базовое упражнение для груди. Вот основные моменты:
- Лягте на скамью, ноги на полу
- Возьмите штангу хватом шире плеч
- Опускайте медленно до касания груди
- Выжимайте мощным движением
- Держите лопатки сведёнными"

**Пользователь:** "Что делать если болят мышцы после тренировки?"
**AI:** "😌 Болезненность мышц после тренировки — это нормально (DOMS). Рекомендации:
- Отдых и восстановление
- Лёгкая растяжка
- Массаж или тёплая ванна
- Достаточный сон
- Питание с белком для восстановления"

## ⚠️ Важно

1. **Стоимость:** AI API платные (кроме некоторых бесплатных тарифов)
2. **Лимиты:** У каждого провайдера есть лимиты запросов
3. **Приватность:** Сообщения отправляются в AI сервис
4. **Контекст:** AI не имеет доступа к личным данным, только к общему контексту

## 🔧 Настройка на Railway

Добавьте переменные окружения в Railway:

1. Откройте проект в Railway
2. Перейдите в **Variables**
3. Добавьте:
   - `AI_PROVIDER=openai`
   - `OPENAI_API_KEY=sk-...`
   - `OPENAI_MODEL=gpt-3.5-turbo`

## 🎛️ Отключение AI

Чтобы отключить AI:

```env
AI_PROVIDER=none
```

Или просто не добавляйте API ключи.

## 📊 Сравнение провайдеров

| Провайдер | Модель | Стоимость | Скорость | Качество |
|-----------|--------|----------|----------|----------|
| OpenAI | GPT-3.5-turbo | $0.0015/1K токенов | ⚡⚡⚡ | ⭐⭐⭐⭐ |
| OpenAI | GPT-4 | $0.03/1K токенов | ⚡⚡ | ⭐⭐⭐⭐⭐ |
| Yandex | YandexGPT | ~$0.002/1K токенов | ⚡⚡⚡ | ⭐⭐⭐⭐ |
| Anthropic | Claude 3 Haiku | $0.25/1M токенов | ⚡⚡⚡ | ⭐⭐⭐⭐ |

## 🚀 Рекомендации

- **Для начала:** Используйте GPT-3.5-turbo (дешевле и быстрее)
- **Для качества:** GPT-4 или Claude 3 Sonnet
- **Для России:** Yandex GPT (лучшая поддержка русского)

---

**Готово!** Теперь ваш бот может отвечать на вопросы пользователей с помощью AI.

