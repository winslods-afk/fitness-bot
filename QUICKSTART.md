# 🚀 Быстрый запуск бота - Пошаговая инструкция

## Шаг 1: Получение токена бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям: придумайте имя и username для бота
4. Скопируйте полученный токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Установка Python

Убедитесь, что у вас установлен Python 3.11 или выше:

```bash
python --version
```

Если Python не установлен, скачайте с [python.org](https://www.python.org/downloads/)

## Шаг 3: Переход в папку проекта

Откройте терминал (PowerShell или Command Prompt) и перейдите в папку проекта:

```bash
cd C:\Users\Roman\fitness-bot
```

## Шаг 4: Создание виртуального окружения (рекомендуется)

```bash
python -m venv venv
```

Активация виртуального окружения:

**В PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1
```

**В Command Prompt:**
```cmd
venv\Scripts\activate.bat
```

## Шаг 5: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 6: Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   copy .env.example .env
   ```

2. Откройте файл `.env` в любом текстовом редакторе (Notepad, VS Code и т.д.)

3. Замените `your_bot_token_here` на ваш токен от BotFather:
   ```
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

4. Сохраните файл

## Шаг 7: Запуск бота

```bash
python -m app.main
```

Или:

```bash
python app/main.py
```

## Шаг 8: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по username, который вы указали при создании
3. Отправьте команду `/start`
4. Бот должен ответить: "привет, добавь свою программу"

## ✅ Готово!

Бот запущен и готов к работе. Теперь вы можете:
- Добавить программу тренировок
- Начать тренировку
- Просмотреть свои программы

---

## 🐳 Альтернатива: Запуск через Docker

Если у вас установлен Docker:

1. Убедитесь, что файл `.env` создан и заполнен (см. Шаг 6)

2. Запустите:
   ```bash
   docker-compose up -d
   ```

3. Просмотр логов:
   ```bash
   docker-compose logs -f
   ```

4. Остановка:
   ```bash
   docker-compose down
   ```

---

## ❓ Решение проблем

### Ошибка "TELEGRAM_BOT_TOKEN не установлен"
- Проверьте, что файл `.env` существует в папке `fitness-bot`
- Убедитесь, что токен указан правильно (без пробелов, кавычек)

### Ошибка "ModuleNotFoundError"
- Убедитесь, что вы активировали виртуальное окружение
- Выполните `pip install -r requirements.txt` еще раз

### Бот не отвечает
- Проверьте, что бот запущен (нет ошибок в терминале)
- Убедитесь, что токен правильный
- Проверьте интернет-соединение

### Ошибка с базой данных
- Удалите файл `fitness_bot.db` (если он есть) и перезапустите бота
- БД создастся автоматически при первом запуске

---

## 📝 Пример использования

1. Отправьте `/start`
2. Нажмите "Добавить программу"
3. Выберите количество дней (например, 3)
4. Введите название дня: "Спина"
5. Добавьте упражнение: `Подтягивания — 10-8-6`
6. Добавьте еще: `Тяга штанги — 4х8`
7. Отправьте `/done`
8. Повторите для остальных дней
9. Введите название программы: "Моя программа"
10. Нажмите "Начать тренировку" и следуйте инструкциям

---

**Удачных тренировок! 💪**

