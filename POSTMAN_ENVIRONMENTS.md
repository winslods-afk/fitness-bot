# Настройка окружений Postman

## Почему `http://localhost:8000`?

`http://localhost:8000` - это значение по умолчанию для **локальной разработки**.

Когда вы запускаете API сервер командой `python api_server.py`, он автоматически запускается на порту 8000 (или на порту из переменной окружения `PORT`).

## Как изменить base_url?

### Вариант 1: Изменить в Postman Environment

1. Откройте Postman
2. В правом верхнем углу нажмите на выпадающий список окружений
3. Выберите "Fitness Bot API - Local" (или создайте новое)
4. Нажмите на иконку глаза (👁️) рядом с названием окружения
5. Найдите переменную `base_url`
6. Измените значение на нужное:
   - Локально: `http://localhost:8000`
   - Railway: `https://your-app.railway.app`
   - Другой порт: `http://localhost:3000`

### Вариант 2: Создать новое окружение для Railway

1. В Postman нажмите на иконку шестеренки (⚙️) в правом верхнем углу
2. Нажмите **"Add"** для создания нового окружения
3. Назовите его "Fitness Bot API - Railway"
4. Добавьте переменные:
   - `base_url` = `https://your-app.railway.app` (ваш URL на Railway)
   - `api_key` = `dotainstructor`
5. Сохраните

Теперь можно переключаться между окружениями:
- **Local** - для тестирования на вашем компьютере
- **Railway** - для тестирования на production сервере

### Вариант 3: Изменить в файле окружения

Откройте файл `Fitness_Bot_API.postman_environment.json` и измените:

```json
{
  "values": [
    {
      "key": "base_url",
      "value": "https://your-app.railway.app",  // Измените здесь
      "type": "default",
      "enabled": true
    }
  ]
}
```

Затем импортируйте обновленный файл в Postman.

## Разные окружения

### Локальная разработка
```
base_url = http://localhost:8000
```

### Railway (Production)
```
base_url = https://your-app.railway.app
```

### Другой порт локально
Если вы хотите использовать другой порт (например, 3000):
1. Измените в `api_server.py` или установите переменную окружения `PORT=3000`
2. Обновите `base_url` в Postman на `http://localhost:3000`

## Как узнать URL на Railway?

1. Откройте Railway Dashboard
2. Выберите ваш проект
3. Выберите сервис с API
4. Перейдите на вкладку **"Settings"**
5. Найдите раздел **"Domains"** или **"Public URL"**
6. Скопируйте URL (например: `https://fitness-bot-api-production.up.railway.app`)

## Автоматическое определение порта

API сервер автоматически использует:
- Переменную окружения `PORT` (если установлена) - для Railway
- Порт `8000` по умолчанию - для локальной разработки

Это видно в `api_server.py`:
```python
port = int(os.getenv("PORT", 8000))  # 8000 - значение по умолчанию
```

## Примеры использования

### Локально
```json
{
  "base_url": "http://localhost:8000"
}
```

### Railway
```json
{
  "base_url": "https://fitness-bot-api-production.up.railway.app"
}
```

### Кастомный порт
```json
{
  "base_url": "http://localhost:3000"
}
```

## Важно

- `localhost:8000` - только для локальной разработки
- Для production на Railway используйте публичный URL
- Переменная `base_url` в Postman позволяет легко переключаться между окружениями

