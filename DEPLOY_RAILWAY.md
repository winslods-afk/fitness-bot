# 🚂 Быстрый деплой на Railway (5 минут)

Railway - самый простой способ задеплоить бота. Бесплатный тариф, автоматический деплой из GitHub.

## 📋 Что нужно:

1. Аккаунт на GitHub (бесплатно)
2. Аккаунт на Railway (бесплатно)
3. Токен бота от @BotFather

## 🚀 Пошаговая инструкция:

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com) и создайте новый репозиторий
2. Назовите его, например, `fitness-bot`
3. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)

### Шаг 2: Загрузите код в GitHub

Откройте терминал в папке `fitness-bot` и выполните:

```bash
# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit"

# Добавление удаленного репозитория (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/fitness-bot.git

# Отправка кода
git branch -M main
git push -u origin main
```

### Шаг 3: Создайте проект на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите "Start a New Project"
3. Выберите "Deploy from GitHub repo"
4. Авторизуйтесь через GitHub
5. Выберите ваш репозиторий `fitness-bot`
6. Railway автоматически начнет деплой

### Шаг 4: Добавьте токен бота

1. В проекте Railway нажмите на ваш сервис
2. Перейдите во вкладку "Variables"
3. Нажмите "New Variable"
4. Добавьте:
   - **Name:** `TELEGRAM_BOT_TOKEN`
   - **Value:** ваш токен от @BotFather
5. Нажмите "Add"

### Шаг 5: Дождитесь деплоя

Railway автоматически:
- Установит зависимости
- Запустит бота
- Покажет логи

Проверьте вкладку "Deployments" - должен быть статус "Active"

### Шаг 6: Проверьте работу бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить!

## ✅ Готово!

Бот работает на хостинге 24/7. При каждом пуше в GitHub Railway автоматически обновит бота.

## 🔧 Полезные команды:

**Просмотр логов:**
- В Railway: вкладка "Deployments" → выберите деплой → "View Logs"

**Обновление бота:**
```bash
git add .
git commit -m "Update"
git push
```
Railway автоматически задеплоит обновления!

**Остановка бота:**
- В Railway: Settings → Delete Service

## 💡 Советы:

1. **Бесплатный тариф:** Railway дает $5 бесплатно каждый месяц - этого хватит для бота
2. **Логи:** Всегда проверяйте логи при проблемах
3. **База данных:** SQLite файл хранится в контейнере, при перезапуске может сброситься (для продакшена лучше PostgreSQL)
4. **Мониторинг:** Railway показывает использование ресурсов в реальном времени

## ❓ Проблемы?

**Бот не отвечает:**
- Проверьте логи в Railway
- Убедитесь, что токен указан правильно
- Проверьте, что деплой завершился успешно

**Ошибка при деплое:**
- Проверьте, что все файлы загружены в GitHub
- Убедитесь, что `requirements.txt` существует
- Проверьте логи деплоя

**База данных не сохраняется:**
- На бесплатном тарифе Railway может перезапускать контейнер
- Рассмотрите использование PostgreSQL (Railway предоставляет бесплатно)

---

**Удачного деплоя! 🚀**

